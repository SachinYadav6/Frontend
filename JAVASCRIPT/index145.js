/* 
array.forEach((currentValue, index, array) => {
  
});
foreach()
*/

let number = [10, 20, 30];
const num = number.forEach((currentValue, index, array) => {
    console.log(currentValue, index, array);
    
})
     
console.log(num);

   
    

